<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://maven.apache.org/POM/4.0.0"
        xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.huawei.javamesh</groupId>
        <artifactId>javamesh</artifactId>
        <version>1.0.0</version>
    </parent>
    <artifactId>javamesh-plugins</artifactId>
    <packaging>pom</packaging>
    <properties>
        <javamesh.basedir>${pom.basedir}/../..</javamesh.basedir>

        <package.plugin.name>${project.artifactId}</package.plugin.name>
        <package.plugin.version>${javamesh.version}</package.plugin.version>
        <package.plugin.dir>${package.agent.dir}/pluginPackage</package.plugin.dir>
        <package.plugin.type>undefined</package.plugin.type>
        <package.output.dir>${package.plugin.dir}/${package.plugin.name}/${package.plugin.type}</package.output.dir>
        <package.server.output.dir>${package.server.dir}/${package.plugin.name}</package.server.output.dir>

        <config.skip.flag>true</config.skip.flag>
        <config.file.name>config.yaml</config.file.name>
        <config.source.dir>../config</config.source.dir>
        <config.output.dir>${package.plugin.dir}/${package.plugin.name}/config</config.output.dir>
    </properties>
    <profiles>
        <profile>
            <id>agent</id>
            <activation>
                <activeByDefault>true</activeByDefault>
            </activation>
            <modules>
                <module>javamesh-flowcontrol</module>
                <module>javamesh-server-monitor</module>
                <module>javamesh-flowrecord</module>
                <module>javamesh-route</module>
                <module>javamesh-threadlocal</module>
                <module>javamesh-online-stresstest</module>
            </modules>
        </profile>
        <profile>
            <id>example</id>
            <modules>
                <module>javamesh-example</module>
            </modules>
        </profile>
        <profile>
            <id>ext</id>
            <activation>
                <activeByDefault>true</activeByDefault>
            </activation>
            <modules>
                <module>javamesh-flowcontrol</module>
                <module>javamesh-flowrecord</module>
                <module>javamesh-server-monitor</module>
                <module>javamesh-route</module>
            </modules>
        </profile>
        <profile>
            <id>all</id>
            <modules>
                <module>javamesh-example</module>
                <module>javamesh-flowcontrol</module>
                <module>javamesh-server-monitor</module>
                <module>javamesh-flowrecord</module>
                <module>javamesh-route</module>
                <module>javamesh-threadlocal</module>
                <module>javamesh-online-stresstest</module>
            </modules>
        </profile>
    </profiles>

    <build>
        <finalName>${project.artifactId}-${package.plugin.version}</finalName>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-jar-plugin</artifactId>
                    <configuration>
                        <archive>
                            <manifestEntries>
                                <Java-mesh-Plugin-Name>${package.plugin.name}</Java-mesh-Plugin-Name>
                                <Java-mesh-Plugin-Version>${package.plugin.version}</Java-mesh-Plugin-Version>
                            </manifestEntries>
                        </archive>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-resources-plugin</artifactId>
                    <executions>
                        <execution>
                            <phase>package</phase>
                            <goals>
                                <goal>copy-resources</goal>
                            </goals>
                            <configuration>
                                <skip>${config.skip.flag}</skip>
                                <outputDirectory>${config.output.dir}</outputDirectory>
                                <overwrite>true</overwrite>
                                <resources>
                                    <resource>
                                        <directory>${config.source.dir}</directory>
                                        <includes>
                                            <include>${config.file.name}</include>
                                        </includes>
                                    </resource>
                                </resources>
                            </configuration>
                        </execution>
                    </executions>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>
</project>
